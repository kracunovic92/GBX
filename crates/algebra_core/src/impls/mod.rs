//! Implementations of core traits for primitive types.
//!
//! We provide `Zero`, `One`, and `Scalar` for integer primitives, relying on
//! the blanket trait impls in `additive` / `multiplicative` for the rest.

mod primitive_identities;
